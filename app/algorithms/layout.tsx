const AlgorithmsListLayout = ({ children }: Readonly<{ children: React.ReactNode }>) => {
    return <main className="bg-dark-base text-white min-h-svh text-sm">{children}</main>;
};

export default AlgorithmsListLayout;
